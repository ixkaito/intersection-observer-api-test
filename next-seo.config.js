/**
 * @type {import('next-seo/lib/types').NextSeoProps}
 **/
const config = {
  titleTemplate: '%s | Intersection Observer Performance Test',
  openGraph: {
    type: 'article',
    locale: 'en_US',
    site_name: 'Intersection Observer Performance Test',
  },
  twitter: {
    cardType: 'summary_large_image',
  },
}

export default config
